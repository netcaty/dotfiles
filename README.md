# v2ex屏蔽器

V2EX 增强油猴脚本（`v2ex-blocker.user.js`）：按关键词过滤首页和节点页的帖子，
另含 Base64 内容自动解码、回复框实时预览与图片粘贴上传。

## 安装

Tampermonkey 中新建脚本，粘贴 `v2ex-blocker.user.js` 的内容，或从 GreasyFork 安装。

## 更新

每次发版必须递增脚本头部的 `@version`，GreasyFork 靠它判断是否有新版本。
